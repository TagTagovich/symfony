<form method="post" action="{{ path('product_photo_delete', {'id': product_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_photo.id) }}">
    <button class="btn">Удалить</button>
</form>
