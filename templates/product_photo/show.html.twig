{% extends 'base.html.twig' %}

{% block title %}ProductPhoto{% endblock %}

{% block body %}
    <h1>ProductPhoto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_photo.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ product_photo.imageName }}</td>
            </tr>
            <tr>
                <th>ImageSize</th>
                <td>{{ product_photo.imageSize }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ product_photo.updatedAt ? product_photo.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>TestField</th>
                <td>{{ product_photo.testField }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_photo_index') }}">back to list</a>

    <a href="{{ path('product_photo_edit', {'id': product_photo.id}) }}">edit</a>

    {{ include('product_photo/_delete_form.html.twig') }}
{% endblock %}
