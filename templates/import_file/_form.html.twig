{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Импортировать новые товары') }}</button>
    <button class="btn btn-success">{{ button_label|default('Обновить свойства товаров') }}</button>
{{ form_end(form) }}
