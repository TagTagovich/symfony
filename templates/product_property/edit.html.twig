{% extends 'base.html.twig' %}

{% block title %}Изменение свойств продукта{% endblock %}

{% block body %}
    <h4 class="d-flex justify-content-between align-items-center w-100 font-weight-bold py-3 mb-4">
        <div><a href="{{ path('product_property_index') }}">Свойства продукта</a> / Изменение свойств продукта</div>
    </h4>
    <div class="card">
        <div class="card-body">
            {{ include('product_property/_form.html.twig', {'button_label': 'Обновить'}) }}
        </div>
    </div>
{% endblock %}