<form method="post" action="{{ path('categoraa_delete', {'id': categoraa.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить эту директорию?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categoraa.id) }}">
    <button class="btn">Удалить</button>
</form>
