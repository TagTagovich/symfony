{% extends 'base.html.twig' %}

{% block title %}Изменение пароля{% endblock %}

{% block body %}
	<div class="card">
        <div class="card-body">
		    {{ form_start(form) }}
		    {{ form_widget(form) }}
		    {{ form_end(form) }}
        </div>
	</div>
{% endblock %}
